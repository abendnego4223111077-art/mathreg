{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildEnvironment": "V3",
    "env": {
      "RAILPACK_PHP_VERSION": "8.3"
    }
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan optimize && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}